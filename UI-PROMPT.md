你是资深移动端产品设计师。请为「dsh-mobile」——一个连接桌面 DSH 的联邦手机客户端 App——产出整套 UI 原型。画布 390×844，浅色模式，中文界面。所有颜色严格使用下方色板，禁止出现其他色系。

━━━ 设计语言 ━━━
· 主色 Klein Blue #002FA7；柔蓝 #E8EDFB；背景 iceWhite #F7F9FC；卡片白 #FFFFFF；输入填充 #EFF2F8；主文字 #1A1F2B；次级 #5A6472；弱化 #8A93A0；分隔线 #E4E8EE；成功 #1B8A5A；错误 #C0392B；警示 #B8860B；强调蓝 #3167C4
· 圆角：输入 12；卡片 14-16；聊天气泡 18；按钮 14；全圆用于发送钮/新建钮/回底浮钮/状态点
· 字体层级：页面大标题 28 w800；会话标题 16 w600；正文/消息 15 行高1.45；次级 13；信息 12；徽标 10-11；凭证/路径/工具行等宽 mono 12-13
· 间距网格 4/8/12/16/24/32；动效 120ms(按压)/200ms(切换)/250ms(浮层)/300ms(慢)；流式文字只增量、不闪烁
· 图标 Material Symbols 线性 outlined，线条 1.5-2pt；顶栏 19-22、行内 16-20、空态 44

━━━ 页面 1：加入页（未配对首屏）━━━
垂直居中，内容区最大宽 400。从上到下：①品牌标记 64×64 圆角18 柔蓝底+白 DNS 节点图标；②标题「连接你的 dsh」26 w800 居中；③副文案「在 dsh 桌面「设置 → 联邦」生成配对码，把手机接入同一台 dsh」13 次级 居中；④白色卡片（圆角16 描边分隔线 阴影 0 6 16 rgba(26,31,43,.06)）内三个输入框（48 高、填充底、圆角12、label 13 次级、hint 14 弱化：地址=example.com / 配对码=XXXX-XXXX 字距2 / 设备名=我的 iPhone），全宽按钮「加入」48 高 主色底 白字16 w600 圆角14；⑤错误文案红色13 可拷贝，显示在按钮下方。
· 状态：加入中按钮内白色20 spinner；失败恢复按钮并显示错误。

━━━ 页面 2：三 Tab 容器 + 设置页（已配对）━━━
· 底部 NavigationBar（64高 白底 选中态柔蓝胶囊+主色图标）：标签 会话 / 文件 / 设置；首屏=会话 Tab。
· 设置页：头部「Mac mini」24 w700 + 状态行（8px 圆点 成功色/灰 +「已连接 · 我的 iPhone」13 弱化）→「联邦连接设置」入口卡（dns 图标 20 主色 + 标题 14 w600 + chevron）→「可连接的设备」段头 13 w600 弱化 → 设备行 56 高（computer 图标 22 在线主色/离线灰 + 名称 16 w600 + 副行「dsh 节点 · 在线/凭证待上传」12 弱化 + chevron 20；当前设备名称后缀「· 当前」）→「退出联邦」13 弱化居中 → 红色确认弹窗。
· 状态：连接出错 → 状态点改 错误色 + 文案「连接出错」。

━━━ 页面 3：会话列表 ━━━
· 顶栏：左 dsh 名 28 w700（点击切换）；右刷新 21 弱化（加载时替换 16 spinner）+「+」36×36 全圆主色白加号。
· 搜索框 38 高 填充底圆角12 前置 search 18 弱化，有内容时右侧 clear。
· 分组列表：组头（chevron18+项目名14 w600 次级+总数12 弱化+可选徽标「含 N 子智能体」10 圆角胶囊填充底），点击折叠；默认展开、每组预览5条、超出显示「显示全部 N 条」文字按钮13 次级。
· 会话行约64高：标题16 w600 单行省略；副行12 弱化相对时间（刚刚/N分钟前/N小时前/昨天/M-d/yyyy-MM-dd）；子智能体会话缩进40、13灰+smart_toy 13 前置；分隔线0.5 左缩进20。
· 状态：空「暂无会话」居中15 弱化；错误（cloud_off 44+文案14+「重试」主色）；刷新失败保留旧列表+SnackBar。

━━━ 页面 4：会话详情（聊天页，核心）━━━
· 顶栏56：返回←｜标题16 w600 单行省略｜右侧：群聊/单聊切换（groups⇄chat_bubble 19）、工具调用开关（construction 19）、连接指示（wifi 15：成功绿=实时连接中/灰=轮询兜底）、停止按钮（仅任务进行中显示，stop_circle 20 错误色）。
· 消息区 padding 12：用户消息右对齐 主色底(#002FA7) 白字 圆角18 无描边；助手消息左对齐 白底 描边分隔线1 圆角18；内 padding 14/10；文字15 行高1.45；最宽78%；流式进行时助手气泡文字逐步追加（可选块状光标 ▍ 1s 闪烁）；工具调用行 13 等宽 弱化 单行省略 前置 build_circle 13；群聊视图：系统旁白居中胶囊（柔蓝底11 斜体 次级）、角色行=头像28 圆（主色16%底 emoji13）+名字10 角色色 w600+气泡13。
· 审批卡（入消息流，渲染在消息列表末尾；出现动效200ms）：白卡 圆角14 描边主色30% padding14；头部 shield 16 主色 +「工具审批请求」13 w700；正文「Agent 请求执行「xxx」：原因」13 次级 行高1.4；操作行双按钮 42 高 圆角10：「拒绝」描边 次级字｜「允许一次」主色填充白字；解析后消失+SnackBar；出现动效200ms下滑。
· 提问卡（入消息流，渲染在消息列表末尾，同卡片风格）：头部 help_outline 16 主色 +「Agent 提问」13 w700；每问题=小标题11 主色 w700（可选）+ 问题13 主色文字 + 选项 chip（24高：未选白底描边分隔线；选中柔蓝底主色描边1.4；支持多选）或自由输入（48高填充底圆角10）；底部「提交回答」主色填充+「取消」描边；未完成校验提示红色12。
· 待处理提示行「N 条消息等待 agent 处理」13 弱化 前置 hourglass_top 13，仅在非空时显示。
· 输入区（底部 SafeArea）：输入框 圆角24 填充底 min44 max5行 hint「发送消息」15 弱化；右侧图片按钮（22 弱化）+ 发送圆钮 40 全圆主色白箭头20（发送中50%透明）；发送后输入清空、用户气泡立即回显；失败回显消失+红色SnackBar。
· 贴底浮钮：右下 40 全圆主色白↓，仅用户上滑远离底部>80px 时出现（200ms上浮），点击回底并收起。
· 状态：loading 中心32 主色 spinner；错误 cloud_off 44+文案+重试；「未选择目标 dsh」空态。

━━━ 页面 5：文件页 ━━━
· 顶栏「文件网关/设备名·根目录」14 单行省略+右刷新。
· 设备选择：说明行12 次级 + 设备卡（白底圆角12 间距8：storage 22 主色/离线灰 + 名14 w600 + 副行 deviceId·在线 12）。
· 浏览器：路径条 11 等宽 弱化；行54高（folder/file 20 警示/主色 + 名13 w500 + 副行「目录/1.2 MB」11 次级）；底部 FAB extended「上一级」主色底；下载中行尾 spinner；空目录居中「（空目录）」14 弱化。

━━━ 页面 6：弹窗与全局 ━━━
· 设备切换 bottom sheet：白底 上圆角16 padding20；标题「切换设备」20 w700；行56（check_circle 20 主色=当前/computer 20 灰；名16 w600+副行「dsh 节点」12）；遮罩40%黑。
· 联邦连接设置 dialog：说明11 次级（「云连接为默认；本地联调时改为 127.0.0.1」）+ brokerUrl/registryUrl 两个等宽12 输入（描边圆角10）+ 取消/保存。
· SnackBar：floating 圆角12 时长4s 文字14；错误红色。
· 系统通知（手机端）：审批/提问/任务失败总是通知；回复完成仅后台；点通知进对应会话。

━━━ 必须做出来的交互（验收点）━━━
1. 聊天页：流式文字从无到有逐步增长，终态只留一条完整气泡（不重复）；
2. 审批横幅出现 → 点「允许一次」→ 横幅消失，后续流式继续；
3. 消息区上滑 → 右下回底浮钮出现；点击 → 回到底部并收起；
4. 顶部 wifi 指示在 绿（实时连接）↔ 灰（轮询兜底）间可切换演示；
5. 主页/列表/聊天三处「在线状态」联动。

━━━ 交付格式 ━━━
输出 8 张高保真竖屏图（390×844）+ 一个 5 屏可点击流程（加入→主页→会话列表→聊天流式+审批→提问横幅）。每张图标注尺寸与颜色值。

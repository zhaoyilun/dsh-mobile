# dsh-mobile UI 设计规格 v2（Klein Blue 版，2026-09-06 裁决后）

> 用途：交给原型工具（Stitch 等）直接还原的完整文字规格。
> 画布基准：390 × 844（iPhone 14 / 常见安卓），浅色模式；深色模式 Phase 2（色板已预留映射）。
> 设计前提：这是「DSH 联邦移动客户端」——手机连接同一台 dsh 桌面（Mac），核心场景：随时收发会话、批准工具、回答 agent 提问、浏览/下载文件。

---

## 1. 设计语言（品牌与 Tokens）

### 1.1 色板（唯一权威，禁止引入风格外颜色）
| 名称 | Hex | 用途 |
|---|---|---|
| brand（Klein Blue） | `#002FA7` | 主行动、选中态、品牌标、图标强调 |
| brandDeep | `#002080` | 按压/渐变深端 |
| brandSoft | `#E8EDFB` | 用户气泡、选中 chip 底、品牌徽标底 |
| bg（iceWhite） | `#F7F9FC` | 全屏底色 |
| surface | `#FFFFFF` | 卡片、助手气泡、弹窗 |
| field | `#EFF2F8` | 输入框/搜索框填充底 |
| ink | `#1A1F2B` | 一级文字 |
| inkSecondary | `#5A6472` | 二级文字 |
| inkMuted | `#8A93A0` | 三级/占位/时间戳 |
| divider | `#E4E8EE` | 分隔线、描边 |
| success | `#1B8A5A` | 在线、成功 |
| error | `#C0392B` | 错误、停止、危险操作 |
| warning | `#B8860B` | 目录/警示 |
| info | `#3167C4` | 信息强调 |

### 1.2 字体
- 系统默认（iOS SF / Android Roboto / Web 思源黑体回退）；可读性优先
- 层级：页面大标题 28 w800 / 会话标题 16 w600 / 正文消息 15 行高 1.45 / 次级 13 / 信息 12 / 徽标 10-11
- 等宽（monospace）：凭证、路径、工具调用行（12-13 灰）

### 1.3 圆角 / 间距 / 动效
- 圆角：输入 12、卡片 14-16、气泡 18、按钮 14、全圆（发送钮/浮钮/状态点）
- 间距网格：4 / 8 / 12 / 16 / 24 / 32（基准 8）
- 动效：按压反馈 120ms、页面切换/展开 200ms、浮层 250ms、慢 300ms（不做多余动画）；流式文字**只增量不加闪烁**

### 1.4 图标
- Material Symbols 线性风格（outlined），线条 1.5-2pt，尺寸：顶栏 19-22、行内 16-20、空态 44

---

## 2. 信息架构（页面树）

```
加入（首屏，仅未配对）
 └─ 三 Tab（已配对，2026-09-06 裁决）
     ├─ 会话（首屏直达：会话列表 ── 会话详情/聊天页，核心）
     ├─ 文件（设备选择 → 文件浏览）
     └─ 设置（本机状态/联邦连接设置/可连接设备/退出联邦）
全局：底部设备切换（bottom sheet）、SnackBar、系统通知
```

---

## 3. 逐页规格

### 3.1 加入页（未配对首屏，唯一动作）
- **布局**：垂直居中，内容区 maxWidth 400；从上到下：
  1. 品牌标记：64×64 圆角 18 品牌软底 + 白色 DNS/节点图标 28
  2. 标题「连接你的 dsh」26 w800 居中
  3. 副文案「在 dsh 桌面「设置 → 联邦」生成配对码，把手机接入同一台 dsh」13 inkSecondary 行高 1.4 居中
  4. **卡片**（白底、圆角 16、描边 divider、阴影 `0 6 16 rgba(26,31,43,0.06)`），内：
     - 输入① 地址（label 13 inkSecondary；默认提示 example.com）
     - 输入② 配对码（label 配对码；**letterSpacing 2**；提示 XXXX-XXXX）
     - 输入③ 设备名（label 设备名；默认「我的 iPhone/Android/浏览器」）
     - 主按钮「加入」：48 高、brand 底白字 16 w600、圆角 14、全宽
  5. 错误文案（红 13，可长按拷贝），置于按钮下方
- **输入框规格**：48 高、field 填充底、圆角 12、无边框；聚焦时描边 brand 1.4；hint 14 inkMuted
- **状态**：加入中 → 按钮内 20px 白 spinner；失败 → 按钮恢复 + 错误文案
- **动效**：进入 200ms 轻微上浮；错误文案出现 150ms fade

### 3.2 三 Tab 容器与设置页（已配对）
- **顶栏**（padding 20/18）：左「Mac mini」28 w800（**可点，弹设备切换 bottom sheet**）+ 状态行（8px 圆点 success/muted +「已连接 · 我的 iPhone」13 inkMuted）｜右齿轮 22 inkMuted（联邦连接设置）
- **主行动区**：两个 52 高全宽按钮，间距 10：
  - 「会话」：brand 填充、白字 16 w600、圆角 26、前置 chat_bubble 20；点击→会话列表
  - 「文件」：白底、ink 字、描边 divider 1、圆角 26、前置 folder 20；点击→文件页
  - 未选择目标 dsh 时：两钮 40% 透明禁用
- **「可连接的设备」**：段头 13 w600 inkMuted → 设备行（56 高：computer 图标 22 在线品牌/离线 muted；名称 16 w600 + 副行「dsh 节点 · 在线/凭证待上传」12 inkMuted；右侧 chevron_right 20 muted；当前设备名称后补「· 当前」标签）
- **底部**：「退出联邦」13 inkMuted 居中 → 红色确认弹窗
- **状态**：连接出错 → 状态点 error 色 + 文案「连接出错」；支持下拉刷新（brand spinner）
- 设备行点击：切换目标 dsh（顶部当前设备变灰提示切换完成 SnackBar）

### 3.3 会话列表
- **顶栏**：左 dsh 名 28 w700（点击→设备切换 sheet）｜右侧：刷新（21 inkMuted；加载时替换为 16px spinner）+ 新建「+」**36×36 全圆 brand** 白加号
- **搜索**：38 高、field 填充、圆角 12、search 前 18 muted；有内容时右侧 clear 16
- **分组列表**（核心交互）：
  - 组头（56 高）：chevron(18) + 项目名（14 w600 inkSecondary）+ 总数（12 muted）+ 可选徽标「含 N 子智能体」（10 pill、field 底）
  - 点击组头折叠/展开；默认展开且每组**预览 5 条**，超出显示「显示全部 N 条」text 按钮（13 inkSecondary）
  - 会话行（约 64 高）：标题 16 w600 单行省略；第二行 12 inkMuted 相对时间（刚刚/N 分钟前/N 小时前/昨天/M-d/yyyy-MM-dd）；子智能体会话：缩进 40、13 inkMuted 灰 + 前置 smart_toy 13、点击弹解释 SnackBar
  - 分隔线 0.5 divider（左缩进 20）；组间留 10
- **状态**：空（居中「暂无会话」15 inkMuted）；错误（cloud_off 44 + 14 文案 + 「重试」text 品牌色）；刷新失败保留旧列表 + SnackBar

### 3.4 会话详情（聊天页）—— 核心屏
- **顶栏 56**：返回 ← ｜标题 16 w600 单行省略 ｜右侧依次：
  1. 群聊/单聊切换（groups ⇄ chat_bubble 19 muted，tooltip）
  2. 工具调用显示开关（construction 19 muted）
  3. **连接指示**：wifi 15（绿 success = 实时连接中 / 灰 inkMuted = 轮询兜底，tooltip 说明）
  4. 停止（仅任务进行中显示：stop_circle 20 error）
- **消息区**（padding 12，自适应滚动，贴底）：
  - 用户消息：右对齐，**brand（#002FA7）底、白字（2026-09-06 裁决）**、圆角 18、无描边；内 padding 14/10；文字 15 行高 1.45；最宽 78%
  - 助手消息：左对齐，白底、描边 divider 1、圆角 18；文字同上
  - **流式进行中**：助手样式气泡，文字逐步追加（无闪烁；可选块状光标「▍」1s step 闪烁）
  - 工具调用行：13 mono inkMuted 单行省略，前置 build_circle 13；顶栏开关控制显隐
  - 群聊视图：系统旁白居中 pill（brandSoft 底 11 italic inkSecondary）；角色行=头像 28 圆（品牌 16% 底 emoji 13）+ 名字 10 角色色 w600 + 气泡 13 白描边
- **审批卡**（入消息流，2026-09-06 裁决；渲染在消息列表末尾）：
  - 白卡、圆角 14、描边 brand 30%、padding 14、出现动效 200ms 下滑
  - 头部：shield 16 brand + 「工具审批请求」13 w700
  - 正文：「Agent 请求执行「xxx」：原因」13 inkSecondary 行高 1.4
  - 操作：行内双按钮 42 高圆角 10——「拒绝」描边（inkSecondary 字）｜「允许一次」brand 填充白字
  - 解析后消失 + SnackBar（✅ 已允许 / 已拒绝）；键盘弹出时横幅保持在消息区上方
- **提问卡**（入消息流，渲染在消息列表末尾，同卡片风格）：
  - 头部：help_outline 16 brand + 「Agent 提问」13 w700
  - 内容：每问题=小标题 11 brand w700（可选）+ 问题正文 13 ink + 选项 **chip**（24 高：未选白底描边 divider、选中 brandSoft 底 brand 描边 1.4；multiSelect 支持多选）——或自由输入（48 高 field 底圆角 10）
  - 底部：「提交回答」brand 填充｜「取消」描边；提交校验（必答问题未完成时「请完成所有问题再提交」红 12）
- **待处理提示**：「N 条消息等待 agent 处理」13 inkMuted 行（hourglass_top 13），仅在 queue 非空时出现
- **输入区**（底部 SafeArea，padding 12/6/10）：
  - 输入框：圆角 24、field 底、min 44 高、max 5 行、hint「发送消息」15 inkMuted、无边框
  - 右侧：图片按钮（add_photo_alternate 22 inkMuted，发送中禁用）+ **发送圆钮 40 全圆 brand**（arrow_upward 20 白）；发送中 50% 透明
  - 发送即回显：输入清空、用户气泡立即出现（负 seq 临时）；失败：回显消失 + red SnackBar「发送失败: …」
- **贴底浮钮**：右下 40 全圆 brand 白 ↓，仅当用户上滑（距底 >80px）出现（200ms 上浮）；点击回到底部并收起
- **状态**：loading（中心 32 brand spinner）；错误（cloud_off 44 + 14 文案 + 重试按钮）；「未选择目标 dsh」同空态样式
- **数据流提示**（原型文案即可）：打开即本地历史即刻渲染 → 网络合并 → SSE 增量；右上连接指示实时反映

### 3.5 文件页
- **顶栏**：「文件网关」（未选设备时）/「设备名 根目录」14 单行省略 ｜右刷新
- **设备选择**：说明行 12 inkSecondary（「选择要浏览/传输文件的设备（跨设备文件走原生命名空间）」）→ 设备卡（白底圆角 12、间距 8：storage 22 品牌/离线 muted + 名 14 w600 + 副行 deviceId · 在线 12）
- **浏览器**：路径条（11 mono inkMuted，`~/Downloads/dsh-incoming` 样式）→ 行（54 高：folder/file 20 warning/brand + 名 13 w500 + 副行「目录/1.2 MB」11 inkSecondary）
- 底部 FAB extended「上一级」（brand 底）；下载中行尾小 spinner；完成 SnackBar
- 空目录：居中「（空目录）」14 inkMuted；错误同会话页空态风格

### 3.6 弹窗与全局组件
- **设备切换 bottom sheet**：白底、上圆角 16、padding 20；标题「切换设备」20 w700；行 56（check_circle 20 品牌=当前 / computer 20 muted；名 16 w600 + 副行「dsh 节点」12 muted）；背景遮罩 40% 黑
- **联邦连接设置（dialog）**：说明 11 inkSecondary（「云连接为默认；本地联调时改为 127.0.0.1」）+ brokerUrl/registryUrl 两个 mono 12 输入（描边圆角 10）+ 取消/保存
- **SnackBar**：floating 圆角 12、4s、14 文案；危险红/默认 ink；不遮挡输入区
- **系统通知（Android 由客户端发）**：审批、提问、任务失败总是通知；回复完成仅后台；点通知进入对应会话

---

## 4. 状态机与边界（原型演示建议）
1. 在线 ↔ 离线：主页状态点、顶栏连接指示、设备行 三处联动
2. SSE 断：wifi 灰 + 5s 轮询（页面正常用，右下可提示「已走轮询兜底」一次性 SnackBar）
3. 会话内到达审批：横幅出现 → 允许 → 横幅消失 → 助手继续流式
4. 发送失败：回显撤回 + 红提示；弱网超时（图片 60s）

---

## 5. 原型建议（Stitch 可直接照做）
- **画布 390×844，先做 8 屏**：①加入 ②主页（在线态）③会话列表（含分组/子智能体/搜索）④聊天-静态（单聊满屏含工具行）⑤聊天-流式+审批卡 ⑥聊天-提问卡 ⑦文件-设备页 ⑧设备切换 sheet
- **关键交互必须做出来**（评价设计好坏的）：④→⑤流式增长；⑤审批「允许一次」→ 横幅消失；贴底浮钮（上滑出现/点击回底）；顶栏 wifi 灰↔绿
- **一句话验收标准**：把原型做出来配到真机看三点——单手够得着（发送钮/浮钮/底栏）、信息不打架（审批/提问/流式同时出现仍清晰）、Klein Blue 一眼是 DSH

---

## 6. 附录：与当前实现的对应关系（确保原型=可实现）
- 配色/间距/圆角：`lib/theme.dart` DshTokens + `session_list_page.dart` Ui（已统一）
- 聊天数据流：本地库先渲染 → session.history 合并 → SSE 增量（chunk/text 双通道）→ 终态 `assistant/message` 单气泡
- 审批/提问：host `/api/respond` 协议级支持（允许一次/拒绝/选项/自由文本/取消）
- 文件：files.claim 取件单 + sha256 校验流式落盘（Web=Blob 下载）
- 已知视觉欠账：暗色主题（Phase 2 按 §1 映射做）、群聊角色配色（按名称 hash 稳定）
